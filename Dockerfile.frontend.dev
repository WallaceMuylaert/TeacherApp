# Frontend Dockerfile para DESENVOLVIMENTO
# Hot reload habilitado com Vite dev server
FROM node:20-alpine

WORKDIR /app

# Copiar apenas package.json para instalar dependências
COPY frontend/package*.json ./
RUN npm ci

# Não copia o código fonte - será montado via volume
# COPY frontend/ .

EXPOSE 5173

# Rodar servidor de desenvolvimento do Vite com hot reload
# --host para permitir conexões externas ao container
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
